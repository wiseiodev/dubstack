{
  "meta": {
    "workId": "DUB-78",
    "title": "Tree-aware dub merge-check and dub ready (tree coverage + tests)",
    "artifactId": "dub-78",
    "repo": "wiseiodev/dubstack",
    "date": "2026-05-24",
    "branch": "feature/dub-78-tree-aware-dub-merge-check-and-dub-ready-tree-coverage-tests",
    "baseBranch": "main",
    "author": "Daniel Wise <daniel@wiseio.dev>",
    "contextLabel": "Linear",
    "contextId": "DUB-78",
    "prUrl": "https://github.com/wiseiodev/dubstack/pull/57"
  },
  "stats": {
    "fileCount": 9,
    "additions": 625,
    "deletions": 64,
    "diffStat": "9 files changed, 625 insertions(+), 64 deletions(-)"
  },
  "prompt": "DUB-78 makes dub merge-check and dub ready tree-aware after DUB-20 dropped the branching-blocker rejection in submit. Both commands now accept a --scope flag (current | downstack | stack) so trees with siblings can be validated end-to-end. Reviewers should confirm: (1) merge-check and ready succeed cleanly on tree-shaped stacks, (2) the scope flag selects the right branches per mode, (3) per-branch findings are aggregated when multiple branches in scope fail without losing recovery hints when only one branch fails, (4) existing linear-stack tests and the single-PR error shape are preserved.",
  "tldr": "Adds `--scope <current|downstack|stack>` to dub merge-check and dub ready, a shared resolveScopeBranches helper, and tree-fixture tests covering all three scopes plus multi-branch failure aggregation.",
  "why": {
    "motivation": [
      "Tier 1 — Branching Stack Support: now that submit walks trees natively (DUB-20), merge-check and ready must follow so the full mergeability story works on trees.",
      "Without scope semantics, running merge-check on a branch with siblings silently ignored those siblings even though they need merging in stack order too."
    ],
    "before": [
      "dub merge-check only ever inspected the current branch's PR (or an explicit --pr).",
      "dub ready called getSubmitPlan({ path: 'current' }) with no way to widen to siblings.",
      "There was no shared vocabulary for 'current branch + ancestors' vs 'whole stack' across commands."
    ],
    "after": [
      "dub merge-check --scope downstack|stack walks multiple branches and prints per-branch findings; --pr/--branch keep their single-PR semantics.",
      "dub ready --scope <mode> selects the validation breadth; default is downstack (current + ancestors), matching the issue recommendation.",
      "Shared lib/scope.ts (ScopeMode + parseScope + resolveScopeBranches) gives future commands one helper for tree-aware scoping."
    ]
  },
  "fileTour": [
    {
      "path": "packages/cli/src/lib/scope.ts",
      "changeType": "new",
      "additions": 73,
      "deletions": 0,
      "explanation": "New helper: ScopeMode union, parseScope (DubError with recovery hints on bad input), and resolveScopeBranches which returns the branch list anchored on currentBranch for current/downstack and falls back to topologicalOrder(stack) BFS for stack. Root branches are always filtered out.",
      "snippet": "export type ScopeMode = 'current' | 'downstack' | 'stack';\n\nexport function resolveScopeBranches(\n  stack: Stack,\n  currentBranch: string,\n  scope: ScopeMode,\n): Branch[] {\n  if (scope === 'stack') {\n    return topologicalOrder(stack).filter((b) => b.type !== 'root');\n  }\n  // ... current + downstack walk via parent chain\n}",
      "snippetLanguage": "ts",
      "open": true
    },
    {
      "path": "packages/cli/src/lib/scope.test.ts",
      "changeType": "new",
      "additions": 78,
      "deletions": 0,
      "explanation": "Unit coverage for parseScope (accepts the three values, throws DubError otherwise) and resolveScopeBranches (3-sibling tree fixture: main → feat/a → {feat/b1, feat/b2, feat/b3}). Covers root-branch and missing-branch edge cases.",
      "snippet": "",
      "snippetLanguage": "",
      "open": false
    },
    {
      "path": "packages/cli/src/commands/merge-check.ts",
      "changeType": "mod",
      "additions": 144,
      "deletions": 14,
      "explanation": "Refactor: extracts checkPrFinding (returns a per-branch finding instead of throwing) and throwIfAnyFailed (preserves single-PR DubError shape when exactly one branch fails — regardless of how many were inspected — and aggregates otherwise). New options.scope path walks resolveScopeBranches over the current stack when no --pr/--branch is given.",
      "snippet": "if (failed.length === 1) {\n  const [only] = failed;\n  if (!only) return;\n  throw new DubError(only.reason, only.fixes);\n}",
      "snippetLanguage": "ts",
      "open": true
    },
    {
      "path": "packages/cli/src/commands/merge-check.test.ts",
      "changeType": "mod",
      "additions": 152,
      "deletions": 17,
      "explanation": "Existing single-PR throw assertions retained verbatim. Adds a tree-shaped fixture (3 siblings) and five new tests: downstack scope walks current+ancestors; stack scope walks every non-root branch (real topologicalOrder via vi.importActual — no longer mocked); current scope checks only the current branch; --branch overrides scope walking; multi-branch failure aggregates the summary message.",
      "snippet": "",
      "snippetLanguage": "",
      "open": false
    },
    {
      "path": "packages/cli/src/commands/ready.ts",
      "changeType": "mod",
      "additions": 22,
      "deletions": 4,
      "explanation": "Accepts options.scope (default downstack). Maps scope=stack to getSubmitPlan({ path: 'stack' }) and scope=current to a single-branch slice of the plan; downstack preserves the prior getSubmitPlan({ path: 'current' }) behavior. Result type adds scope + submitPath fields.",
      "snippet": "const planPath: SubmitPathMode = scope === 'stack' ? 'stack' : 'current';\nconst plan = await getSubmitPlan(cwd, { path: planPath });\nsubmitBranches =\n  scope === 'current'\n    ? planBranches.filter((name) => name === plan.currentBranch)\n    : planBranches;",
      "snippetLanguage": "ts",
      "open": false
    },
    {
      "path": "packages/cli/src/commands/ready.test.ts",
      "changeType": "mod",
      "additions": 75,
      "deletions": 0,
      "explanation": "Existing tests preserved. Adds three scope tests on a 3-sibling tree: downstack returns current + ancestors and forwards { path: 'current' }; current narrows to just the current branch; stack returns every non-root branch and forwards { path: 'stack' }.",
      "snippet": "",
      "snippetLanguage": "",
      "open": false
    },
    {
      "path": "packages/cli/src/index.ts",
      "changeType": "mod",
      "additions": 65,
      "deletions": 4,
      "explanation": "CLI wiring: imports parseScope/ScopeMode; adds --scope option to ready (default downstack) and merge-check (default current; --pr forces single-PR mode), each with addHelpText examples. merge-check now prints one line per branch when result.branches.length > 1.",
      "snippet": "",
      "snippetLanguage": "",
      "open": false
    },
    {
      "path": "README.md",
      "changeType": "mod",
      "additions": 20,
      "deletions": 4,
      "explanation": "Updates the dub ready and dub merge-check sections with the new --scope flag, default values, and usage examples.",
      "snippet": "",
      "snippetLanguage": "",
      "open": false
    },
    {
      "path": "apps/docs/content/docs/commands/doctor.mdx",
      "changeType": "mod",
      "additions": 14,
      "deletions": 3,
      "explanation": "Mirrors the README updates in the docs site so the rendered command reference shows the --scope examples for ready and merge-check.",
      "snippet": "",
      "snippetLanguage": "",
      "open": false
    }
  ],
  "reviewFocus": [
    {
      "title": "throwIfAnyFailed preserves single-PR DubError shape",
      "location": "packages/cli/src/commands/merge-check.ts (throwIfAnyFailed)",
      "rationale": "Adversarial review caught that the original check only preserved the per-finding fixes when findings.length === 1. The current implementation triggers on failed.length === 1 instead, so a single failing branch inside a multi-branch scope still throws DubError(reason, fixes) with the correct recovery hints rather than the aggregated summary."
    },
    {
      "title": "resolveScopeBranches contract for scope=stack",
      "location": "packages/cli/src/lib/scope.ts:resolveScopeBranches",
      "rationale": "scope=stack ignores currentBranch by design (every non-root branch is returned). Confirm the JSDoc clearly states this so future callers don't pass a stray currentBranch expecting an anchor."
    },
    {
      "title": "Real topologicalOrder exercised by merge-check tree tests",
      "location": "packages/cli/src/commands/merge-check.test.ts (vi.mock '../lib/state.js')",
      "rationale": "Uses vi.importActual to keep topologicalOrder un-mocked so the BFS sibling sort runs for real. Verify the mock surface only stubs readState + findStackForBranch."
    },
    {
      "title": "Default scope choice on each command",
      "location": "packages/cli/src/index.ts (ready and merge-check definitions)",
      "rationale": "ready defaults to downstack (matches issue recommendation; preserves prior submit-plan path); merge-check defaults to current (preserves the prior single-PR check semantics for the CLI default invocation). Confirm both defaults are explicit in the help text."
    }
  ],
  "testPlan": [
    {
      "kind": "unit",
      "label": "scope.test.ts (parseScope + resolveScopeBranches)",
      "status": "done",
      "evidence": "vitest src/lib/scope.test.ts → 7 tests passing; covers each scope on a 3-sibling tree plus root/missing-branch edge cases."
    },
    {
      "kind": "unit",
      "label": "ready.test.ts scope coverage",
      "status": "done",
      "evidence": "vitest src/commands/ready.test.ts → 6 tests passing (3 original + 3 new scope tests asserting forwarded submit path)."
    },
    {
      "kind": "unit",
      "label": "merge-check.test.ts tree scope coverage",
      "status": "done",
      "evidence": "vitest src/commands/merge-check.test.ts → 10 tests passing (5 original single-PR + 5 new tree-scope tests including multi-branch aggregation)."
    },
    {
      "kind": "manual",
      "label": "CLI smoke: --scope visible in --help on both commands",
      "status": "done",
      "evidence": "Captured in .reports/dub-78-qa.md: ready --help and merge-check --help both show the new --scope option with the correct default."
    }
  ],
  "qualityGates": [
    {
      "name": "Lint + format",
      "command": "pnpm checks",
      "status": "passed",
      "evidence": "biome check . → 248 files, 0 errors."
    },
    {
      "name": "Typecheck",
      "command": "pnpm typecheck",
      "status": "passed",
      "evidence": "turbo run typecheck → 2 packages successful (dubstack + docs)."
    },
    {
      "name": "Tests",
      "command": "pnpm test",
      "status": "passed",
      "evidence": "vitest → 85 files, 704 tests passing including the new scope, ready, and merge-check tree coverage."
    }
  ],
  "selfQa": {
    "tsxChanged": false,
    "videoRequired": false,
    "videoRequiredReason": "No .tsx files modified; the change is CLI-only and has no browser-demoable surface.",
    "fallbackApprovedByUser": true,
    "mode": "fallback",
    "artifactPath": ".reports/dub-78-qa.md",
    "caption": "CLI smoke transcript and per-AC verification in lieu of video.",
    "scenarios": [
      "dub merge-check --scope stack on a 3-sibling tree iterates every non-root branch and aggregates per-branch findings.",
      "dub ready --scope downstack returns current + ancestors and forwards { path: 'current' } to getSubmitPlan; --scope stack forwards { path: 'stack' }.",
      "dub merge-check default (--scope current, no --pr/--branch) preserves the prior single-PR error message + recovery fixes."
    ]
  },
  "acceptanceCriteria": [
    {
      "criterion": "dub merge-check and dub ready succeed (no blocker error) on tree-shaped stacks after DUB-20 lands",
      "status": "satisfied",
      "evidence": "DUB-20 already landed (commit 86303e2); merge-check.test.ts 'stack scope walks every non-root branch including siblings' and ready.test.ts 'stack scope checks every branch in the stack' both pass on the 3-sibling fixture without any branching-blocker error."
    },
    {
      "criterion": "--scope <mode> flag works on both commands",
      "status": "satisfied",
      "evidence": "ready.test.ts asserts the correct submit-plan path is forwarded for each scope; merge-check.test.ts asserts the right branches are walked for each scope; CLI --help output confirmed in qa fallback."
    },
    {
      "criterion": "Tests for each scope on a 3-sibling tree",
      "status": "satisfied",
      "evidence": "scope.test.ts uses main → feat/a → {feat/b1, feat/b2, feat/b3} and pins each scope; merge-check.test.ts and ready.test.ts reuse the same shape through the public command surface."
    },
    {
      "criterion": "No regression on existing linear-stack tests",
      "status": "satisfied",
      "evidence": "All 5 original single-PR merge-check tests and all 3 original ready tests retained and pass unchanged; total suite 704/704."
    }
  ],
  "adversarialReview": {
    "iterations": 1,
    "critical": 0,
    "major": 0,
    "minor": 1,
    "nitpick": 0,
    "findings": [
      "Critical (resolved): throwIfAnyFailed silently swapped error format when a single failure occurred in a multi-branch scope, stripping per-finding fixes from the thrown DubError. Loosened the guard to failed.length === 1 so the original DubError(reason, fixes) shape is preserved regardless of scope size.",
      "Important (resolved): resolveScopeBranches JSDoc claimed an empty list for missing currentBranch in all scopes, but scope=stack ignores currentBranch entirely. Docstring rewritten to make the per-scope contract explicit.",
      "Important (resolved): merge-check.test.ts mocked topologicalOrder, so the scope-walk ordering assertions relied on a pre-sorted mock return rather than the real BFS. Switched to vi.importActual so the real topologicalOrder runs; the tree tests now exercise the actual scope-walking code path."
    ]
  },
  "dependencies": [
    {
      "name": "DUB-20 (Tree-walking submit) — Linear blocker",
      "status": "Merged: commit 86303e2 'feat(submit): walk tree-shaped stacks without rejection [DUB-20] (#53)'. With the branching-blocker rejection gone, merge-check/ready can now walk trees freely."
    }
  ],
  "rollout": {
    "summary": "Standard merge to main. No data migration, no flag, no env change. The new --scope option is purely additive; existing dub ready and dub merge-check invocations keep working (ready's default matches the previous implicit submit-plan path; merge-check's default remains the single-PR check).",
    "steps": [
      {
        "when": "merge",
        "label": "Land PR",
        "details": "Squash-merge once CI is green; CLI ships in the next dub release."
      },
      {
        "when": "post-merge",
        "label": "Verify downstream commands unaffected",
        "details": "merge-next and post-merge consume mergeCheck/ready outputs; the result type only adds fields (scope, branches), so existing consumers keep working without changes."
      }
    ]
  },
  "commit": {
    "sha": "d34f166202c4e460090234c22d9172d006ea3275",
    "shortSha": "d34f166",
    "message": "feat(merge-check,ready): tree-aware --scope flag [DUB-78]"
  }
}
