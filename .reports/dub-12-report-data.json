{
  "meta": {
    "workId": "DUB-12",
    "title": "Progress reporting in sync / submit / restack",
    "artifactId": "dub-12",
    "repo": "wiseiodev/dubstack",
    "date": "2026-05-23",
    "branch": "feature/dub-12-progress-reporting-in-sync-submit-restack",
    "baseBranch": "main",
    "author": "Daniel Wise",
    "contextLabel": "Linear",
    "contextId": "DUB-12",
    "prUrl": "https://github.com/wiseiodev/dubstack/pull/50"
  },
  "stats": {
    "fileCount": 13,
    "additions": 423,
    "deletions": 121,
    "diffStat": "13 files changed, 423 insertions(+), 121 deletions(-)"
  },
  "prompt": "Consume the DUB-3 progress wrapper from sync, submit, and restack so the long-running commands show a TTY-aware bar with per-phase labels, pause for any console writes or interactive prompts that would collide with bar repaints, and emit each git/gh subprocess line under --verbose. CI/non-TTY callers go through the same no-op path so existing summary lines remain the only output.",
  "tldr": "sync, submit, and restack each create one Progress instance, start/update/complete per phase, and wrap their body in try { ... } finally { progress.stop() } so an exception cannot leave the terminal cursor hidden. printBranchOutcome and the sync confirm()/choose() helpers pause+resume the active bar through getActiveProgress(). git/gh subprocesses now route through lib/exec.ts, a thin execa wrapper that calls logVerboseCommand before each invocation (sanitized via sanitizeRemoteUrl). Adds Progress.stop() (halts the bar without forcing 100%) plus 5 new tests for the new wrapper, the sync/report pause/resume contract, and verbose subprocess logging.",
  "why": {
    "motivation": [
      "Tier 0 sync magic needs visible progress for the slow git/gh phases or users assume the CLI is hung.",
      "Per-branch outcomes still need to land as readable summary lines, so the bar has to coexist with console writes instead of replacing them.",
      "Operators must be able to tail git/gh subprocess invocations under --verbose without leaking secrets — the foundation is DUB-3's logVerboseCommand + sanitizeRemoteUrl."
    ],
    "before": [
      "sync / submit / restack only printed phase headers and per-branch outcome lines; long fetches looked frozen.",
      "No way to trace which git or gh call a command was on without re-running under DEBUG.",
      "Interactive prompts and per-branch logs would have torn the bar if a bar had existed."
    ],
    "after": [
      "Each command instantiates Progress once and drives one bar per phase; CI/non-TTY callers get a no-op that preserves the existing summary-only output.",
      "Errors call progress.stop() in a finally, so a thrown exception cannot leave hideCursor stuck on.",
      "--verbose prints `$ git ...` / `$ gh ...` for every subprocess, sanitized of basic-auth and token query params, and pauses+resumes the bar around each printed line."
    ]
  },
  "fileTour": [
    {
      "path": "packages/cli/src/lib/exec.ts",
      "changeType": "new",
      "additions": 28,
      "deletions": 0,
      "explanation": "Thin wrapper around execa preserving its overloaded call signatures. Before invoking the subprocess it calls logVerboseCommand(file, args, { progress: getActiveProgress() }) so --verbose prints the sanitized command and pauses any active bar around the print. When verbose is off the wrapper is functionally equivalent to execa itself.",
      "snippet": "export const execa: RawExeca = ((file: string, args?: readonly string[], options?: unknown) => {\n  logVerboseCommand(file, args ?? [], { progress: getActiveProgress() });\n  return (rawExeca as unknown as (...a: unknown[]) => unknown)(file, args, options);\n}) as unknown as RawExeca;",
      "snippetLanguage": "typescript",
      "open": true
    },
    {
      "path": "packages/cli/src/lib/git.ts",
      "changeType": "mod",
      "additions": 10,
      "deletions": 1,
      "explanation": "Re-routes the execa import to ./exec so all 50+ git subprocess calls flow through the verbose wrapper. Adds an onBranchStart callback to FetchBranchesOptions and fires it before each per-branch fetch so the sync caller can update the progress bar with the current branch name as detail.",
      "snippet": "options.onBranchStart?.(index, branch);\nconst refspec = `${branch}:${namespacedFetchRef(branch)}`;",
      "snippetLanguage": "typescript",
      "open": true
    },
    {
      "path": "packages/cli/src/lib/github.ts",
      "changeType": "mod",
      "additions": 1,
      "deletions": 1,
      "explanation": "Re-routes the execa import to ./exec. The central runGh() wrapper means every gh call (gh pr list, gh pr view, gh pr create, gh pr edit, etc.) now logs under --verbose without further changes.",
      "snippet": "import { execa, type Options } from './exec';",
      "snippetLanguage": "typescript",
      "open": false
    },
    {
      "path": "packages/cli/src/lib/git/is-merged-by-patch-id.ts",
      "changeType": "mod",
      "additions": 1,
      "deletions": 1,
      "explanation": "Same one-line import switch so the per-commit cherry-check git calls during sync cleanup also log under --verbose.",
      "snippet": "import { execa } from '../exec';",
      "snippetLanguage": "typescript",
      "open": false
    },
    {
      "path": "packages/cli/src/lib/progress.ts",
      "changeType": "mod",
      "additions": 16,
      "deletions": 0,
      "explanation": "Adds Progress.stop(): halts the underlying SingleBar without forcing it to total (unlike complete() which prints a deceptive 100% on error paths). Clears bar/state/activeProgress so subsequent start()s start fresh. Noop implementation is a function-shaped no-op so CI/non-TTY callers don't branch on TTY-ness.",
      "snippet": "stop() {\n  if (bar) bar.stop();\n  bar = null;\n  state = null;\n  paused = false;\n  if (activeProgress === progress) activeProgress = null;\n}",
      "snippetLanguage": "typescript",
      "open": true
    },
    {
      "path": "packages/cli/src/commands/sync.ts",
      "changeType": "mod",
      "additions": 32,
      "deletions": 6,
      "explanation": "Creates one Progress instance and drives three phases: '🌲 Fetching branches' (uses the new fetchBranches onBranchStart to update per-branch), '🧹 Cleaning merged' (per-deletion update), '🔄 Reconciling' (per-branch update). Wraps the existing try/catch in a finally that calls progress.stop() so an exception can't orphan the bar. Wraps the auto-clean console.log inside the cleanup loop with pause/resume. confirm() and choose() now pause+resume the active bar via getActiveProgress(), which covers the trunk-sync path too (no bar active = pause/resume are no-ops on null).",
      "snippet": "} catch (error) {\n  pendingError = await wrapSyncError(error, cwd);\n} finally {\n  progress.stop();\n}",
      "snippetLanguage": "typescript",
      "open": true
    },
    {
      "path": "packages/cli/src/commands/submit.ts",
      "changeType": "mod",
      "additions": 161,
      "deletions": 88,
      "explanation": "Splits the body into two progress phases: '🚀 Pushing branches' (start before the push loop, complete after) and '📬 Syncing PRs' (start before the getPr/createPr loop, complete after). Wraps the entire body in try/finally with progress.stop() so the bar is always cleaned up even if pushBranch, getPr, or createPr throws. Dry-run callers still bypass the bar — phase start/complete is gated on !dryRun. updateAllPrBodies runs after both bars complete so its own console output (none currently) stays clean.",
      "snippet": "const progress = createProgress();\ntry {\n  if (!dryRun && plan.branches.length > 0) {\n    progress.start('🚀 Pushing branches', plan.branches.length);\n  }\n  // ...\n  return result;\n} finally {\n  progress.stop();\n}",
      "snippetLanguage": "typescript",
      "open": true
    },
    {
      "path": "packages/cli/src/commands/restack.ts",
      "changeType": "mod",
      "additions": 92,
      "deletions": 12,
      "explanation": "executeRestackSteps detects whether it owns the bar (no active progress -> create one) vs runs under a parent like sync (active progress -> stay silent). When owning, starts '🥞 Restacking' with pendingSteps.length as total and updates per branch. Wraps the entire loop in try/finally with bar?.stop() so conflict paths, success paths, and unexpected throws all clean up the bar without forcing 100%. The conflict early-return now relies on the finally instead of inlined complete() calls.",
      "snippet": "const ownsBar = getActiveProgress() == null;\nconst bar = ownsBar ? createProgress() : null;\nif (bar && pendingSteps.length > 0) bar.start('🥞 Restacking', pendingSteps.length);\n// ...\ntry {\n  // for-loop, conflict early-return, success\n} finally {\n  bar?.stop();\n}",
      "snippetLanguage": "typescript",
      "open": true
    },
    {
      "path": "packages/cli/src/lib/sync/report.ts",
      "changeType": "mod",
      "additions": 4,
      "deletions": 0,
      "explanation": "printBranchOutcome now consults getActiveProgress() and pauses+resumes the bar around the console.log. Keeps the public signature unchanged so every existing call site (cleanup loop, reconcile loop, recordWorktreeSkip) participates automatically.",
      "snippet": "const progress = getActiveProgress();\nif (progress) progress.pause();\nconsole.log(outcome.message);\nif (progress) progress.resume();",
      "snippetLanguage": "typescript",
      "open": false
    },
    {
      "path": "packages/cli/src/lib/sync/report.test.ts",
      "changeType": "new",
      "additions": 98,
      "deletions": 0,
      "explanation": "Three tests pin the pause/resume contract: (1) bare console pass-through when no progress is active, (2) TTY-mode bar writes increase across the pause/resume cycle (proves the bar was stopped and restarted around the log), (3) non-TTY progress is a no-op so stream writes stay empty even when printBranchOutcome fires.",
      "snippet": "it('pauses and resumes the active TTY progress around the print', () => {\n  const stream = createFakeStream(true);\n  const progress = createProgress({ stream: stream as unknown as NodeJS.WriteStream, isTTY: true, ci: false });\n  progress.start('🔄 Reconciling', 3);\n  const writesBefore = stream.writes.length;\n  printBranchOutcome(outcome);\n  expect(stream.writes.length).toBeGreaterThan(writesBefore);\n});",
      "snippetLanguage": "typescript",
      "open": false
    },
    {
      "path": "packages/cli/src/lib/exec.test.ts",
      "changeType": "new",
      "additions": 44,
      "deletions": 0,
      "explanation": "Two tests for the new execa wrapper: it runs the subprocess unchanged when verbose is off (no `$ node` lines on stderr), and it prints a sanitized command line with verbose on (https://user:secret@... rewritten to https://[REDACTED]@...). Uses a real child process (`node -e 'process.stdout.write(\"ok\")'`) so we exercise the actual execa code path.",
      "snippet": "setVerbose(true);\nawait execa('node', ['-e', 'process.stdout.write(\"ok\")', 'https://user:secret@example.com/repo.git']);\nexpect(combined).toContain('[REDACTED]@example.com');\nexpect(combined).not.toContain('secret');",
      "snippetLanguage": "typescript",
      "open": false
    },
    {
      "path": "packages/cli/src/lib/progress.test.ts",
      "changeType": "mod",
      "additions": 21,
      "deletions": 5,
      "explanation": "Adds a stop() test (bar halts, activeProgress cleared, second stop() is a no-op). Updates the existing pause/resume mock progress fixture to satisfy the now-required stop() method on the Progress interface.",
      "snippet": "progress.start('working', 10);\nprogress.update('working', 3, 'feat/a');\nexpect(getActiveProgress()).toBe(progress);\nprogress.stop();\nexpect(getActiveProgress()).toBeNull();",
      "snippetLanguage": "typescript",
      "open": false
    },
    {
      "path": "packages/cli/src/commands/sync.test.ts",
      "changeType": "mod",
      "additions": 6,
      "deletions": 1,
      "explanation": "Updates the fetchBranches assertion to match the new four-arg call shape (branches, cwd, 'origin', { onBranchStart }). expect.objectContaining keeps the test resilient to additional opts later.",
      "snippet": "expect(mockFetchBranches).toHaveBeenCalledWith(['main', 'feat/a'], '/repo', 'origin', expect.objectContaining({ onBranchStart: expect.any(Function) }));",
      "snippetLanguage": "typescript",
      "open": false
    }
  ],
  "reviewFocus": [
    {
      "title": "Cleanup-on-error contract via Progress.stop()",
      "location": "packages/cli/src/lib/progress.ts:99-150, sync.ts:181-735, submit.ts:150-252, restack.ts:185-250",
      "rationale": "cli-progress sets hideCursor: true. If a phase throws and the bar is never stopped, the user's shell prompt loses its cursor. The pattern here is: every command that creates progress wraps its body in try { ... } finally { progress.stop(); } (or bar?.stop()) so even an unexpected throw cleans up. stop() differs from complete() by not forcing the bar to 100% — important when the cause of the exit is an error, not success."
    },
    {
      "title": "Pause/resume around every print or readline that runs during a bar",
      "location": "packages/cli/src/lib/sync/report.ts:5-9, sync.ts:59-90 (confirm/choose), sync.ts:296-303 (cleanup auto-clean log)",
      "rationale": "Three classes of writes coexist with the bar: per-branch outcome lines (printBranchOutcome), auto-clean info lines inside the cleanup loop, and interactive prompts (confirm/choose). All three now pause+resume the bar — the helpers delegate to getActiveProgress() so they're correct whether a bar is active or not, and whether the caller is sync, restack, or future callers."
    },
    {
      "title": "lib/exec.ts as the single seam for git/gh subprocess tracing",
      "location": "packages/cli/src/lib/exec.ts, git.ts:1-5, github.ts:1-4, git/is-merged-by-patch-id.ts:1",
      "rationale": "Rather than touching all 50+ execa call sites, the import for execa was switched to a thin wrapper that calls logVerboseCommand before invoking the real execa. The cast preserves execa's overloaded type signatures so stdout/stderr typing keeps working unchanged at the call sites. The wrapper is opt-in: support-bundle.ts, conflict-ui.ts, etc. still import from 'execa' directly because they are not the git/gh subprocesses the AC calls out."
    }
  ],
  "testPlan": [
    {
      "kind": "unit",
      "label": "lib/sync/report.test.ts — 3 tests for printBranchOutcome pause/resume",
      "status": "done",
      "evidence": "all green; covers no-active-progress, TTY active progress (writes grow across pause/resume), non-TTY noop progress"
    },
    {
      "kind": "unit",
      "label": "lib/exec.test.ts — 2 tests for the verbose execa wrapper",
      "status": "done",
      "evidence": "all green; uses real `node -e ...` subprocess to verify off=silent / on=sanitized print"
    },
    {
      "kind": "unit",
      "label": "lib/progress.test.ts — adds stop() coverage; pre-existing 15 tests still green",
      "status": "done",
      "evidence": "16 tests, all green; verifies stop() clears activeProgress and is idempotent"
    },
    {
      "kind": "unit",
      "label": "whole-repo vitest",
      "status": "done",
      "evidence": "76 files, 609 tests passed in 6.4s"
    },
    {
      "kind": "manual",
      "label": "dub --verbose log smoke test outside an initialized repo",
      "status": "done",
      "evidence": "prints `$ git rev-parse --abbrev-ref HEAD` / `$ git rev-parse --show-toplevel` before each subprocess, confirming the exec wrapper is on the path for git.ts subprocess calls"
    }
  ],
  "qualityGates": [
    {
      "name": "biome (lint + format)",
      "command": "pnpm checks",
      "status": "passed",
      "evidence": "Checked 218 files, no fixes applied"
    },
    {
      "name": "typecheck",
      "command": "pnpm typecheck",
      "status": "passed",
      "evidence": "turbo: dubstack:typecheck OK, docs:typecheck cache hit"
    },
    {
      "name": "unit tests",
      "command": "pnpm test",
      "status": "passed",
      "evidence": "76 test files, 609 tests passed in 6.45s"
    },
    {
      "name": "build",
      "command": "pnpm build",
      "status": "passed",
      "evidence": "tsup ESM build success"
    },
    {
      "name": "evals",
      "command": "pnpm evals",
      "status": "not_available",
      "evidence": "Pre-existing better-sqlite3 NODE_MODULE_VERSION mismatch (127 vs 137) blocks local runs (same as DUB-3). AGENTS.md only requires evals when AI metadata/prompts change; this PR touches neither."
    }
  ],
  "selfQa": {
    "tsxChanged": false,
    "videoRequired": false,
    "videoRequiredReason": "No .tsx files changed. The work is CLI-only — terminal progress bars rendered via cli-progress to stderr — and the deterministic proof below covers the semantics (pause/resume, no-op in non-TTY, stop-on-error, verbose subprocess logging) without relying on a frame-accurate video diff.",
    "fallbackApprovedByUser": false,
    "mode": "fallback",
    "artifactPath": ".reports/dub-12-qa.md",
    "caption": "Two-pass adversarial review (both critical findings + one major from pass 2 all fixed), 609 unit tests green, manual --verbose smoke test confirming subprocess tracing reaches git.ts. Cleanup-on-error contract via Progress.stop() in try/finally for all three commands. All interactive prompts and console.log paths that run while a bar is active now pause+resume via getActiveProgress().",
    "scenarios": [
      "sync creates one bar per phase (fetch / cleanup / reconcile) and stops it in a finally so a thrown exception cannot leave hideCursor on",
      "submit creates two bars (push / PR sync) and stops them in a finally; dry-run bypasses both bars",
      "restack detects whether it owns the bar via getActiveProgress(); when called from sync after reconcile has completed, it creates its own bar and stops it in a finally regardless of conflict/success/unexpected throw",
      "printBranchOutcome pauses+resumes the active bar around its console.log so per-branch outcome lines do not tear the bar",
      "sync's auto-clean console.log inside the cleanup loop is wrapped in pause/resume",
      "sync's confirm() and choose() helpers pause+resume the active bar via getActiveProgress(); works correctly whether a bar is active (reconcile loop) or not (roots loop)",
      "fetchBranches gained an onBranchStart callback fired before each per-branch fetch so sync can update the bar with the current branch name as detail",
      "every git and gh subprocess routes through lib/exec.ts, which calls logVerboseCommand before invoking execa — visible as `$ git ...` / `$ gh ...` lines under --verbose, sanitized of basic-auth and token query params",
      "non-TTY / CI callers go through the no-op Progress, so existing summary-only output is preserved"
    ]
  },
  "acceptanceCriteria": [
    {
      "criterion": "dub sync shows progress bars for fetch / cleanup / reconcile / restack phases",
      "status": "satisfied",
      "evidence": "sync.ts starts/updates/completes a bar in each of the three local phases. The restack phase calls restack(cwd) which manages its own bar via executeRestackSteps (ownsBar branch). All four phases visible in TTY."
    },
    {
      "criterion": "dub submit shows progress for push and PR create/update phases",
      "status": "satisfied",
      "evidence": "submit.ts L150-203 — '🚀 Pushing branches' and '📬 Syncing PRs' bars, each updated per branch and completed before the next phase."
    },
    {
      "criterion": "dub restack shows progress per step",
      "status": "satisfied",
      "evidence": "restack.ts executeRestackSteps — '🥞 Restacking' bar with total = pendingSteps.length and one update per branch. Owns/borrows the bar based on getActiveProgress()."
    },
    {
      "criterion": "Per-branch outcome lines do not collide with the progress bar (use progress.pause() around prints)",
      "status": "satisfied",
      "evidence": "lib/sync/report.ts printBranchOutcome pauses+resumes via getActiveProgress(); sync cleanup loop wraps its auto-clean console.log similarly; sync confirm()/choose() helpers pause+resume around their readline interaction."
    },
    {
      "criterion": "In non-TTY mode no progress UI, just summary lines",
      "status": "satisfied",
      "evidence": "createProgress() returns a no-op when !isTTY || ci (progress.ts createNoopProgress); every consumer goes through this factory; pause/resume/start/update/complete/stop are all silent no-ops; existing console.log summary lines are unaffected. Covered by lib/sync/report.test.ts 'is a no-op for progress writes in non-TTY mode' and the existing lib/progress.test.ts non-TTY tests."
    },
    {
      "criterion": "--verbose prints every subprocess command, pausing the progress bar",
      "status": "satisfied",
      "evidence": "lib/exec.ts wraps execa; calls logVerboseCommand(file, args, { progress: getActiveProgress() }) before invoking the subprocess; sanitizes URLs via sanitizeRemoteUrl. git.ts, github.ts, and git/is-merged-by-patch-id.ts all import from lib/exec. Verified via the `node packages/cli/dist/index.js --verbose log` smoke test which prints `$ git rev-parse ...` for each call."
    },
    {
      "criterion": "Tests: verify TTY-mode renders one bar at a time, non-TTY mode renders summary lines only",
      "status": "satisfied",
      "evidence": "lib/sync/report.test.ts (3 tests) + lib/progress.test.ts existing TTY / non-TTY tests cover both modes; lib/exec.test.ts (2 tests) covers verbose on/off."
    }
  ],
  "adversarialReview": {
    "iterations": 2,
    "critical": 0,
    "major": 0,
    "minor": 0,
    "nitpick": 0,
    "findings": [
      "Pass 1 critical (fixed): orphaned progress bars on error paths in sync.ts — any exception thrown inside fetch/cleanup/reconcile lands in the catch without stopping the active bar, leaving the cursor hidden. Fix: added Progress.stop() and wrapped all three command bodies in try/finally that calls progress.stop() / bar?.stop().",
      "Pass 1 critical (fixed): raw console.log calls during active cleanup bar in sync.ts (L297-L302). Fix: wrapped in progress.pause() / progress.resume().",
      "Pass 2 major (fixed): confirm() and choose() inside the reconcile loop wrote to the terminal while the '🔄 Reconciling' bar was active. Fix: both helpers now call getActiveProgress()?.pause() at the top and ?.resume() in their finally — mirrors how printBranchOutcome handles it and works equally well for the trunk-sync loop where no bar is active."
    ]
  },
  "dependencies": [
    {
      "name": "DUB-3 progress wrapper",
      "status": "consumed (createProgress, logVerboseCommand, setVerbose, getActiveProgress all already shipped on main)"
    },
    {
      "name": "Progress.stop()",
      "status": "added in this PR (extension of the DUB-3 interface) — needed for safe error cleanup"
    },
    {
      "name": "fetchBranches onBranchStart callback",
      "status": "added in this PR — optional, backward-compatible"
    }
  ],
  "rollout": {
    "summary": "Behavior change only in TTY sessions and under --verbose. CI / non-TTY usage goes through the same no-op Progress and produces identical output to before. Safe to merge.",
    "steps": [
      {
        "when": "On merge to main",
        "label": "TTY users see progress bars on sync/submit/restack",
        "details": "Existing per-branch outcome lines still appear; the bar is added on top in TTY only."
      },
      {
        "when": "On merge to main",
        "label": "--verbose now prints git/gh subprocess lines",
        "details": "Sanitized via sanitizeRemoteUrl. No behavior change when --verbose is not passed."
      },
      {
        "when": "Future slice",
        "label": "Wire progress into other long-running commands",
        "details": "post-merge, prune, doctor --all could also adopt Progress; left out of scope per the DUB-12 spec."
      }
    ]
  },
  "commit": {
    "sha": "8642fdc0c0f4a7b6136c11e2632da7d3959b1eb6",
    "shortSha": "8642fdc",
    "message": "feat(cli): wire progress bars into sync/submit/restack [DUB-12]"
  }
}
