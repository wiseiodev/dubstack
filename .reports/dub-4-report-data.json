{
  "meta": {
    "workId": "DUB-4",
    "title": "Namespaced fetch refs + --no-tags + git remote prune",
    "artifactId": "dub-4",
    "repo": "dubstack",
    "date": "2026-05-23",
    "branch": "feature/dub-4-namespaced-fetch-refs-no-tags-git-remote-prune",
    "baseBranch": "main",
    "author": "Daniel Wise",
    "contextLabel": "Linear",
    "contextId": "DUB-4",
    "prUrl": ""
  },
  "stats": {
    "fileCount": 5,
    "additions": 305,
    "deletions": 3,
    "diffStat": "5 files changed, 305 insertions(+), 3 deletions(-)"
  },
  "prompt": "Adopt Graphite's fetch discipline so `dub sync` no longer clobbers the user's `git fetch` state: write fetched branch tips to a `refs/dubstack/fetch-head/<branch>` namespace, pass `--no-write-fetch-head --no-tags`, and `git remote prune origin` between fetch and trunk pull.",
  "tldr": "`dub sync` now leaves the user's `FETCH_HEAD` untouched, skips tag transfer, and clears deleted-remote-branch ghost refs before fast-forwarding trunk. Fetched tips land in a private `refs/dubstack/fetch-head/<branch>` namespace; the same opportunistic update keeps `origin/<branch>` current for downstream sync logic.",
  "why": {
    "motivation": [
      "Users running `git fetch` manually expect `FETCH_HEAD` to reflect THEIR last fetch, not DubStack's background sync.",
      "On repos with many release tags, `git fetch origin <branch>` pulls every reachable tag, which is wasted bandwidth. `--no-tags` matches Graphite's v1.6.2 optimization.",
      "Stale `refs/remotes/origin/<branch>` ghost refs from deleted-remote branches confuse trunk fast-forward and the cleanup heuristics."
    ],
    "before": [
      "`fetchBranches` ran `git fetch <remote> <branch>`, which writes `FETCH_HEAD` and pulls all reachable tags.",
      "Sync never pruned the remote, so branches deleted on origin still appeared via stale tracking refs.",
      "No private fetch namespace existed; nothing to clean up at sync start."
    ],
    "after": [
      "`fetchBranches` runs `git fetch --no-write-fetch-head --no-tags -f origin <branch>:refs/dubstack/fetch-head/<branch>`, leaving FETCH_HEAD alone and skipping tags.",
      "Sync calls `clearStaleNamespacedFetchRefs` at the top of the run, deleting any `refs/dubstack/fetch-head/*` entry whose source branch is no longer tracked in DubStack state.",
      "Sync calls `git remote prune origin` once between fetch and trunk fast-forward."
    ]
  },
  "fileTour": [
    {
      "path": "packages/cli/src/lib/git.ts",
      "changeType": "mod",
      "additions": 89,
      "deletions": 2,
      "explanation": "Exports the `DUBSTACK_FETCH_REF_PREFIX` constant + `namespacedFetchRef` helper. Rewrites `fetchBranches` to use the namespaced refspec with `--no-write-fetch-head --no-tags -f`. Adds `listNamespacedFetchRefs`, `deleteRef`, `clearStaleNamespacedFetchRefs`, and `pruneRemote` so `sync` can drive the namespace cleanup and remote prune.",
      "snippet": "export const DUBSTACK_FETCH_REF_PREFIX = 'refs/dubstack/fetch-head/';\n\nexport async function fetchBranches(branches, cwd, remote = 'origin') {\n  if (branches.length === 0) return;\n  for (const branch of branches) {\n    const refspec = `${branch}:${namespacedFetchRef(branch)}`;\n    await execa('git', ['fetch', '--no-write-fetch-head', '--no-tags', '-f', remote, refspec], { cwd });\n  }\n}",
      "snippetLanguage": "typescript",
      "open": true
    },
    {
      "path": "packages/cli/src/commands/sync.ts",
      "changeType": "mod",
      "additions": 9,
      "deletions": 0,
      "explanation": "Imports the two new git helpers. Computes the keep-set from the full state (`state.stacks`, not just the scoped stacks) so a partial `dub sync` never deletes namespaced refs that belong to other tracked stacks. Calls `clearStaleNamespacedFetchRefs` at the top of the `try` block, then `pruneRemote('origin', cwd)` after `fetchBranches` and before the trunk fast-forward loop.",
      "snippet": "const allTrackedBranches = new Set(\n  state.stacks.flatMap((s) => s.branches.map((b) => b.name)),\n);\nawait clearStaleNamespacedFetchRefs(allTrackedBranches, cwd);\n\nconsole.log('🌲 Fetching branches from remote...');\nconst toFetch = [...new Set([...roots, ...stackBranches])];\nif (toFetch.length > 0) {\n  await fetchBranches(toFetch, cwd);\n  result.fetched = toFetch;\n}\n\nawait pruneRemote('origin', cwd);",
      "snippetLanguage": "typescript",
      "open": true
    },
    {
      "path": "packages/cli/src/lib/git.test.ts",
      "changeType": "mod",
      "additions": 127,
      "deletions": 1,
      "explanation": "Real-git integration tests for the four behaviors: namespaced ref written, FETCH_HEAD untouched, no tags fetched, missing remote refs skipped. Plus unit-style coverage for `clearStaleNamespacedFetchRefs` (delete-only-stale) and `pruneRemote` (happy path + bad-remote DubError).",
      "snippet": "it('leaves the user FETCH_HEAD untouched (--no-write-fetch-head)', async () => {\n  const fetchHeadPath = path.join(dir, '.git', 'FETCH_HEAD');\n  const manualContent = 'sentinel value written by user\\n';\n  fs.writeFileSync(fetchHeadPath, manualContent);\n  await fetchBranches(['feat/a'], dir);\n  expect(fs.readFileSync(fetchHeadPath, 'utf8')).toBe(manualContent);\n});",
      "snippetLanguage": "typescript",
      "open": false
    },
    {
      "path": "packages/cli/src/commands/sync.test.ts",
      "changeType": "mod",
      "additions": 52,
      "deletions": 0,
      "explanation": "Mocks the two new git helpers and adds an ordering test that asserts `clearStale → fetch → prune → trunk fast-forward` and that the keep-set passed to `clearStaleNamespacedFetchRefs` is the full state, not just the scoped stacks.",
      "snippet": "expect(callOrder.indexOf('clearStale')).toBeLessThan(callOrder.indexOf('fetch'));\nexpect(callOrder.indexOf('prune')).toBeLessThan(callOrder.indexOf('ff:main'));",
      "snippetLanguage": "typescript",
      "open": false
    },
    {
      "path": "packages/cli/test/helpers.ts",
      "changeType": "mod",
      "additions": 28,
      "deletions": 0,
      "explanation": "Adds `attachBareRemote(localDir)` helper that creates a bare repo in tmp and configures it as `origin` on the given local repo, so git.test.ts can drive real fetches end-to-end.",
      "snippet": "export async function attachBareRemote(localDir) {\n  const remoteDir = await fs.promises.mkdtemp(path.join(os.tmpdir(), 'dubstack-test-remote-'));\n  await execa('git', ['init', '-b', 'main', '--bare'], { cwd: remoteDir, env: GIT_TEST_ENV });\n  await execa('git', ['remote', 'add', 'origin', remoteDir], { cwd: localDir, env: GIT_TEST_ENV });\n  return { remoteDir, cleanup: async () => fs.promises.rm(remoteDir, { recursive: true, force: true }) };\n}",
      "snippetLanguage": "typescript",
      "open": false
    }
  ],
  "reviewFocus": [
    {
      "title": "Opportunistic origin/<branch> updates are still relied on",
      "location": "packages/cli/src/commands/sync.ts:175,270 + packages/cli/src/lib/git.ts:fetchBranches",
      "rationale": "Downstream sync logic (`fastForwardBranchToRef`, `hardResetBranchToRef`, `remoteBranchExists`, `isAncestor`) still reads `origin/<branch>` rather than the new namespaced ref. This is intentional and safe — when the explicit refspec `<branch>:refs/dubstack/fetch-head/<branch>` is given, git still opportunistically updates `refs/remotes/origin/<branch>` via the configured `+refs/heads/*:refs/remotes/origin/*` refspec. Verified manually in a scratch repo. Worth a careful eye in review."
    },
    {
      "title": "Keep-set for stale-ref cleanup uses full state, not scoped stacks",
      "location": "packages/cli/src/commands/sync.ts:158-163",
      "rationale": "`clearStaleNamespacedFetchRefs(allTrackedBranches, cwd)` deliberately uses `state.stacks` (every tracked stack), not `scopeStacks`, so a single-stack `dub sync` never deletes namespaced refs belonging to other stacks the user is still tracking. Confirm this matches expected semantics."
    },
    {
      "title": "Prune ordering vs. fetch",
      "location": "packages/cli/src/commands/sync.ts after fetchBranches call",
      "rationale": "`git remote prune origin` runs AFTER `fetchBranches`. The fetch creates/updates namespaced refs and opportunistically refreshes `origin/<branch>`; the subsequent prune only removes `refs/remotes/origin/<branch>` entries that have no counterpart on the remote. This is safe because the just-fetched refs reference branches that DO exist on origin and won't be pruned."
    }
  ],
  "testPlan": [
    {
      "kind": "unit",
      "label": "fetchBranches writes namespaced ref + leaves FETCH_HEAD alone + skips tags + skips missing remote refs",
      "status": "done",
      "evidence": "src/lib/git.test.ts `describe('fetchBranches')` — four real-git tests against a bare remote."
    },
    {
      "kind": "unit",
      "label": "clearStaleNamespacedFetchRefs deletes only refs whose source branch is not in the keep set",
      "status": "done",
      "evidence": "src/lib/git.test.ts `describe('clearStaleNamespacedFetchRefs')` — two tests."
    },
    {
      "kind": "unit",
      "label": "pruneRemote happy path and bad-remote DubError",
      "status": "done",
      "evidence": "src/lib/git.test.ts `describe('pruneRemote')` — two tests."
    },
    {
      "kind": "integration",
      "label": "sync orders clearStale → fetch → prune → trunk fast-forward and passes the full tracked-branch set as keep",
      "status": "done",
      "evidence": "src/commands/sync.test.ts `clears stale namespaced fetch refs and prunes remote once before trunk pull`."
    },
    {
      "kind": "manual",
      "label": "Confirmed in scratch repo that explicit refspec still opportunistically updates refs/remotes/origin/*",
      "status": "done",
      "evidence": "/tmp scratch repos: fetched with the new refspec, observed both `refs/dubstack/fetch-head/feature` and `refs/remotes/origin/feature` updated by a single `git fetch` call."
    }
  ],
  "qualityGates": [
    {
      "name": "biome check",
      "command": "pnpm checks",
      "status": "passed",
      "evidence": "Checked 188 files in 28ms. No fixes applied."
    },
    {
      "name": "typecheck",
      "command": "pnpm typecheck",
      "status": "passed",
      "evidence": "turbo: 2 successful, 2 total. dubstack:typecheck and docs:typecheck both clean."
    },
    {
      "name": "unit tests",
      "command": "pnpm test",
      "status": "passed",
      "evidence": "Test Files: 68 passed (68). Tests: 508 passed (508). Includes 41 in src/lib/git.test.ts (with the new behaviors) and the expanded src/commands/sync.test.ts."
    },
    {
      "name": "evals",
      "command": "pnpm evals",
      "status": "not_available",
      "evidence": "Pre-existing `better-sqlite3` NODE_MODULE_VERSION mismatch in local node_modules. Unrelated to this change; this PR does not touch AI metadata or prompts, so per AGENTS.md evals are not required."
    }
  ],
  "selfQa": {
    "tsxChanged": false,
    "videoRequired": false,
    "videoRequiredReason": "Pure backend change to a CLI tool. No .tsx file changed; no browser-demoable surface.",
    "fallbackApprovedByUser": false,
    "mode": "fallback",
    "artifactPath": ".reports/dub-4-qa.md",
    "caption": "Deterministic proof via test logs and manual scratch-repo verification of opportunistic ref updates.",
    "scenarios": [
      "Fetch a moved remote branch and assert the namespaced ref equals the remote tip.",
      "Pre-write a sentinel to .git/FETCH_HEAD, fetch, assert the sentinel is byte-identical.",
      "Push a tag to remote, fetch, assert `git tag -l` is empty locally.",
      "Create stale and live entries under refs/dubstack/fetch-head/, run cleanup, assert only the stale one is deleted.",
      "Sync ordering: clearStale → fetch → prune → trunk fast-forward, with the full tracked-branch set as keep."
    ]
  },
  "acceptanceCriteria": [
    {
      "criterion": "fetchBranches in lib/git.ts uses namespaced refs and the flag set above",
      "status": "satisfied",
      "evidence": "packages/cli/src/lib/git.ts fetchBranches now runs `git fetch --no-write-fetch-head --no-tags -f origin <branch>:refs/dubstack/fetch-head/<branch>`. Covered by `src/lib/git.test.ts 'writes fetched tip to refs/dubstack/fetch-head/<branch>'`."
    },
    {
      "criterion": "Sync calls git remote prune once before trunk pull",
      "status": "satisfied",
      "evidence": "packages/cli/src/commands/sync.ts calls `pruneRemote('origin', cwd)` between `fetchBranches` and the `for (const root of roots)` trunk fast-forward loop. Ordering asserted in `src/commands/sync.test.ts 'clears stale namespaced fetch refs and prunes remote once before trunk pull'`."
    },
    {
      "criterion": "Stale refs/dubstack/fetch-head/* cleared at sync start",
      "status": "satisfied",
      "evidence": "Top of the sync `try` block calls `clearStaleNamespacedFetchRefs(allTrackedBranches, cwd)`. Behavior covered by `src/lib/git.test.ts 'deletes refs whose source branch is not in the keep set'` and the ordering test in `sync.test.ts`."
    },
    {
      "criterion": "User's own FETCH_HEAD is untouched after dub sync",
      "status": "satisfied",
      "evidence": "`--no-write-fetch-head` on every fetch. `src/lib/git.test.ts 'leaves the user FETCH_HEAD untouched (--no-write-fetch-head)'` writes a sentinel into .git/FETCH_HEAD before fetch and asserts byte-equality after."
    },
    {
      "criterion": "No tags fetched (verify by checking refs after sync against a repo with tags)",
      "status": "satisfied",
      "evidence": "`--no-tags` on every fetch. `src/lib/git.test.ts 'does not fetch remote tags (--no-tags)'` pushes a tag to the remote, runs fetchBranches, and asserts `git tag -l` returns nothing locally."
    },
    {
      "criterion": "Tests for all four behaviors",
      "status": "satisfied",
      "evidence": "All four behaviors plus the missing-remote-ref edge case and the sync-ordering invariant are covered. Total 508/508 tests pass."
    }
  ],
  "adversarialReview": {
    "iterations": 1,
    "critical": 0,
    "major": 0,
    "minor": 0,
    "nitpick": 0,
    "findings": [
      "Reviewer raised theoretical concern that sync still reads `origin/<branch>` rather than the namespaced ref. Investigated and resolved: opportunistic refspec update keeps `origin/<branch>` current, the new `git remote prune origin` actively removes stale tracking refs (strictly safer than the pre-PR behavior), and the issue's scope is explicitly limited to `fetchBranches` and the top of `sync.ts`. No regression vs. pre-PR behavior."
    ]
  },
  "dependencies": [
    {
      "name": "Linear DUB-4 description / acceptance criteria",
      "status": "intake complete"
    },
    {
      "name": "Graphite Charcoal fetch discipline (reference, public)",
      "status": "intake complete"
    },
    { "name": "No external dependencies blocking", "status": "satisfied" }
  ],
  "rollout": {
    "summary": "Standalone behavior change inside `dub sync`. No flag, no migration, no user action required. Existing repos with refs under `refs/dubstack/fetch-head/` (none today) would be cleaned up automatically on the next sync.",
    "steps": [
      {
        "when": "On merge",
        "label": "Merge to main",
        "details": "Standard squash-merge per repo conventions."
      },
      {
        "when": "On next dub sync",
        "label": "Namespace populated",
        "details": "Each fetched branch lands at refs/dubstack/fetch-head/<branch>. User FETCH_HEAD untouched from that point on."
      },
      {
        "when": "On next dub sync after a branch deletion",
        "label": "Stale namespace + tracking refs cleaned",
        "details": "clearStaleNamespacedFetchRefs removes refs for branches no longer tracked; git remote prune origin removes tracking refs for branches deleted on origin."
      }
    ]
  },
  "commit": {
    "sha": "8c8d8dbb907aba40360ce78d1a708d03ddd36a77",
    "shortSha": "8c8d8db",
    "message": "feat(cli): namespaced fetch refs + --no-tags + remote prune in sync"
  }
}
